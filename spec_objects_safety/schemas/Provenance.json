{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.agent-ix.org/agent-ix/spec-objects-safety/0.3.0/Provenance.json",
  "type": "object",
  "properties": {
    "assertedBy": {
      "type": "string",
      "minLength": 1
    },
    "assertedAt": {
      "type": "string",
      "format": "date-time"
    },
    "standard": {
      "type": "string",
      "minLength": 1
    },
    "method": {
      "type": "string",
      "minLength": 1
    },
    "sourceSpan": {
      "$ref": "https://schemas.agent-ix.org/semantic-core/0.1.0/SourceLocus.json"
    }
  },
  "required": [
    "assertedBy",
    "assertedAt"
  ],
  "unevaluatedProperties": {
    "not": {}
  },
  "description": "Who asserted a safety judgement, when, and against what. Required whenever\n`status` is `accepted` (the `allOf`/`if` rule on both record models), together\nwith the assessment or analysis being accepted: risk acceptance with nobody's\nname on it, or with nothing scored to accept, is the one status that must\nnever be reachable by a document simply validating.",
  "additionalProperties": false
}
